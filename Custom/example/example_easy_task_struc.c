struct task_struct {
    pid_t pid;                        // Идентификатор процесса
    long state;                       // Состояние задачи
    struct mm_struct *mm;             // Управление памятью
    struct thread_struct thread;      // Контекст выполнения
    struct signal_struct *signal;     // Управление сигналами
    struct cred *real_cred;           // Реальные полномочия
    struct cred *cred;                // Текущие полномочия
    struct files_struct *files;       // Открытые файлы
    struct fs_struct *fs;             // Файловая система
    int prio;                         // Приоритет задачи
    struct sched_entity se;           // Планировщик
    struct task_struct *real_parent;  // Родительский процесс
    struct task_struct *group_leader; // Лидер группы потоков
    // И другие поля...
};

/*
Основные поля структуры task_struct

Идентификация процесса:
pid_t pid: Уникальный идентификатор процесса.
pid_t tgid: Идентификатор группы задач (группы потоков).

Состояние процесса:
long state: Текущее состояние процесса (например, TASK_RUNNING, TASK_INTERRUPTIBLE, и т.д.).
unsigned int flags: Различные флаги, указывающие состояние или характеристики процесса (например, флаг PF_KTHREAD для потоков ядра).

Управление памятью:
struct mm_struct *mm: Указатель на структуру mm_struct, которая описывает виртуальную память процесса. Для потоков ядра (kernel threads) это поле равно NULL.
struct mm_struct *active_mm: Активная виртуальная память, используемая процессом (для процессов, когда они активны).

Управление потоками (контекст исполнения):
struct thread_struct thread: Содержит архитектурно-зависимые данные, такие как регистры процессора, указатели на стек и контекст выполнения.
struct task_struct *real_parent: Указатель на родительский процесс (по факту того, кто создал процесс).

Запланированное выполнение:
int prio: Приоритет задачи (числовое значение).
int static_prio: Статический приоритет задачи.
struct sched_entity se: Информация для планировщика (scheduler), которая описывает, как задача будет запланирована на выполнение.
struct sched_class *sched_class: Указатель на планировщик, ответственный за планирование задачи.

Время исполнения:
cputime_t utime: Время, проведенное в пользовательском режиме.
cputime_t stime: Время, проведенное в системном режиме (в режиме ядра).
struct timespec start_time: Время, когда процесс был запущен.

Файловые операции:
struct files_struct *files: Указатель на открытые файлы процесса.
struct fs_struct *fs: Указатель на файловую систему процесса (например, текущий каталог, корневой каталог).

Сигналы:
struct signal_struct *signal: Указатель на структуру, содержащую информацию о сигналах для группы процессов.
struct sighand_struct *sighand: Обработчики сигналов для этого процесса.

Права доступа:
const struct cred *real_cred: Оригинальные полномочия процесса.
const struct cred *cred: Текущие полномочия процесса (например, uid, gid).

Управление списками процессов:
struct list_head tasks: Двусвязный список всех задач в системе.
struct task_struct *group_leader: Указывает на лидера группы потоков (если процесс является потоком).

Управление временем процессора:
struct task_cputime cputime_expires: Используется для отслеживания времени процессора для процесса.
u64 sched_time: Время, которое процесс провел в состоянии планирования.

Контекст для работы с памятью:
struct vm_area_struct *mmap: Указатель на список виртуальных областей памяти (используемых в процессе).

Ожидание событий:
wait_queue_head_t wait_chldexit: Список ожидания для дочерних процессов, когда они завершаются.
Структуры, на которые ссылается task_struct

mm_struct:
Описывает виртуальную память процесса.
Включает поля для управления памятью, такие как информация о страницах, таблицы страниц, и список областей памяти (vm_area_struct).
Связана с управлением виртуальной памятью процесса.

signal_struct:
Описывает сигналы, связанные с процессом.
Содержит информацию о состоянии сигналов и их обработчиках.

files_struct:
Содержит информацию об открытых файлах процесса.
Связана с файловой подсистемой.

fs_struct:
Описывает файловую систему процесса (текущий каталог, корневой каталог).

cred и real_cred:
Структуры, описывающие полномочия процесса (uid, gid, списки групп).
Связаны с правами доступа и безопасностью в системе.

thread_struct:
Архитектурно-зависимая структура, описывающая низкоуровневый контекст выполнения (регистры, стек, управление процессором).

sched_entity:
Описывает сущность планирования для задачи. Используется планировщиком для определения, когда и как задаче нужно выделить процессорное время.

sighand_struct:
Содержит информацию о текущих обработчиках сигналов для задачи.